#include <stdio.h>

int main() {
    //printf("Hello, World!\n");
    /*int n, m;
    int map[100][100] = {0};
    int arr[100] = {0};
    int flag = 1;
    scanf("%d %d", &n, &m);
    int x, y;
    for (int i = 0; i < m; ++i) {
        scanf("%d %d", &x, &y);
        map[x-1][y-1] = 1;
        map[y-1][x-1] = 1;
    }
    //arr[0] = 1;
    for (int i = 0; i < n; ++i) {
        for (int j = i; j < n; ++j) {
            if (map[i][j] == 1)
                arr[j] = 1;
        }
    }
    for (int i = 1; i < n; ++i) {
      if (arr[i] == 0)
          flag = 0;
    }
    if(flag == 1)
        printf("yse");
    else
        printf("no");*/


    return 0;
}